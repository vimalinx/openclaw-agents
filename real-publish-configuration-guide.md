# 真实发布配置指南

> 生成时间: 2026-02-21 00:34
> 状态: 准备配置真实发布

---

## 📋 配置清单

### 1. 火山引擎 API 配置

**获取 API Key**：
1. 访问火山引擎控制台：https://console.volcengine.com/
2. 登录火山引擎账号
3. 进入"API 密钥管理"
4. 创建新的 API 密钥
5. 复制 API Key

**配置 API Key**：

**方法 A：环境变量**
```bash
export VOLCENGINE_API_KEY="your_api_key_here"
```

**方法 B：配置文件**
```bash
cd /home/vimalinx/.openclaw/workspace/xiaohongshu-auto-reply
cat > .env <<EOF
VOLCENGINE_API_KEY=your_api_key_here
EOF
```

**方法 C：直接在脚本中配置**
```bash
# 编辑 xhs-auto-pipeline.py
# 添加 API Key 到配置
```

---

### 2. Chrome 远程调试端口配置

**启动 Chrome 远程调试**：
```bash
# 停止现有 Chrome 进程
pkill -f chrome

# 启动新的 Chrome（带远程调试）
google-chrome --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug &
```

**验证端口监听**：
```bash
# 检查 9222 端口是否在监听
netstat -tuln | grep 9222
# 或
lsof -i :9222
```

---

### 3. 小红书登录状态验证

**检查登录状态**：
```bash
# 使用 Playwright 检查
cd /home/vimalinx/.openclaw/workspace/xiaohongshu-auto-reply
python3 -c "
from playwright.sync_api import sync_playwright

with sync_playwright() as p:
    browser = p.chromium.connect_over_cdp('http://localhost:9222')
    default_context = browser.contexts[0]
    pages = default_context.pages
    
    if pages:
        current_url = pages[0].url
        if 'xiaohongshu.com/explore' in current_url or 'xiaohongshu.com/user' in current_url:
            print('✅ 已登录')
        else:
            print('⚠️ 未登录或还在登录页面')
"
```

---

### 4. 修改脚本以启用真实发布

**需要修改的文件**：
- `xhs-auto-pipeline.py` - 主自动化脚本
- `xhs-auto-publisher/skill.py` - 发布技能

**修改内容**：
1. 启用真实图像生成（调用火山引擎 API）
2. 启用真实发布（不使用模拟数据）
3. 添加真实数据收集（浏览量、点赞、收藏、评论）

---

### 5. 测试真实发布流程

**测试命令**：
```bash
cd /home/vimalinx/.openclaw/workspace/xiaohongshu-auto-reply

# 测试模式（使用模拟数据）
python3 xhs-auto-pipeline.py test

# 真实发布模式
python3 xhs-auto-pipeline.py run --topic "AI工具"
```

---

## 🎯 配置步骤总结

### 立即配置（今天）

1. **获取火山引擎 API Key** ⭐⭐⭐⭐⭐
   - 登录火山引擎控制台
   - 创建 API 密钥
   - 复制 API Key

2. **配置环境变量** ⭐⭐⭐⭐
   ```bash
   export VOLCENGINE_API_KEY="your_api_key"
   ```

3. **启动 Chrome 远程调试** ⭐⭐⭐⭐⭐
   ```bash
   google-chrome --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug &
   ```

4. **验证小红书登录** ⭐⭐⭐⭐
   - 在 Chrome 中手动登录
   - 使用 Playwright 验证登录状态

5. **测试真实发布** ⭐⭐⭐
   - 测试内容生成
   - 测试配图生成
   - 测试自动发布

---

## 💡 最佳实践

### 图像生成配置

**推荐设置**：
- **风格**：小红书风格（简洁、明亮、吸引人）
- **尺寸**：3:4 (1080x1440)
- **质量**：高清晰度
- **格式**：JPEG
- **水印**：添加账号名称水印

### 发布时间策略

**最佳发布时间**：
- **工作日**：10:00、14:00、18:00
- **周末**：09:00、11:00、15:00
- **避开**：夜间（22:00-08:00）

### 内容发布频率

**推荐频率**：
- **新账号**：每天 1-2 篇
- **活跃账号**：每天 3-5 篇
- **成熟账号**：每天 5-10 篇

---

## ⚠️ 注意事项

### 安全性

1. **不要频繁发布**
   - 避免被小红书判定为垃圾账号
   - 遵守平台使用规范

2. **内容质量**
   - 确保内容有价值
   - 避免低质内容
   - 保持原创性

3. **账号安全**
   - 不要在公共网络发布
   - 定期更换密码
   - 启用二次验证

### 技术限制

1. **API 限流**
   - 火山引擎 API 有速率限制
   - 注意 API 调用频率
   - 避免触发限流

2. **浏览器资源**
   - Chrome 可能占用较多资源
   - 注意监控内存和 CPU 使用
   - 定期重启浏览器

---

## 📊 预期效果

### 短期（1 周）

- ✅ 发布 10-20 篇笔记
- ✅ 积累 500-1000+ 浏览量
- ✅ 积累 50-100+ 点赞
- ✅ 积累 20-50+ 收藏
- ✅ 积累 10-30+ 评论

### 中期（1 个月）

- ✅ 发布 50-100 篇笔记
- ✅ 积累 2000-5000+ 浏览量
- ✅ 积累 200-500+ 点赞
- ✅ 积累 100-250+ 收藏
- ✅ 积累 50-150+ 评论
- ✅ 增加粉丝 100-300+

### 长期（3 个月）

- ✅ 发布 150-300 篇笔记
- ✅ 建立稳定粉丝基础（500-1000+）
- ✅ 积累 5000-10000+ 浏览量
- ✅ 实现持续的内容输出
- ✅ 建立小红书 IP

---

## 🎯 配置完成后的行动

### 第 1 天

1. **配置火山引擎 API** ⭐⭐⭐⭐⭐
2. **启动 Chrome 远程调试** ⭐⭐⭐⭐
3. **验证小红书登录** ⭐⭐⭐⭐⭐
4. **测试内容生成** ⭐⭐⭐⭐
5. **测试配图生成** ⭐⭐⭐⭐

### 第 2 天

6. **测试自动发布** ⭐⭐⭐⭐
7. **验证发布成功** ⭐⭐⭐⭐
8. **收集反馈数据** ⭐⭐⭐⭐
9. **优化发布策略** ⭐⭐⭐⭐
10. **调整内容模板** ⭐⭐⭐⭐

### 第 3 天以后

11. **启用定时发布** ⭐⭐⭐
12. **增加发布频率** ⭐⭐⭐
13. **建立内容矩阵** ⭐⭐⭐
14. **优化图像生成** ⭐⭐⭐
15. **扩展到其他平台** ⭐⭐⭐

---

## 📄 配置文件

### 火山引擎配置
```bash
# 火山引擎 API Key
VOLCENGINE_API_KEY=your_api_key_here

# 火山引擎 API 地址
VOLCENGINE_API_URL=https://open.volcengine.com/api/v3/text/image/v2

# 模型版本
VOLCENGINE_MODEL=doubao-v3

# 图像参数
VOLCENGINE_WIDTH=1080
VOLCENGINE_HEIGHT=1440
VOLCENGINE_FORMAT=jpeg
VOLCENGINE_QUALITY=high
```

### 小红书配置
```bash
# 小红书账号
XHS_ACCOUNT=11549094412

# 发布策略
XHS_MAX_POSTS_PER_DAY=5
XHS_PUBLISH_DELAY_MIN=300
XHS_PUBLISH_DELAY_MAX=600

# 内容策略
XHS_TITLE_LIMIT=20
XHS_CONTENT_LENGTH=500-1000
XHS_IMAGE_COUNT=3-5
```

---

## 🔧 故障排除

### 问题 1：火山引擎 API 调用失败

**症状**：`Error: Invalid API Key`

**解决方案**：
1. 检查 API Key 是否正确
2. 检查账户是否有足够余额
3. 检查 API 权限是否正确

### 问题 2：图像生成失败

**症状**：`Error: Image generation failed`

**解决方案**：
1. 检查网络连接
2. 检查火山引擎服务状态
3. 检查图像生成参数
4. 减小图像尺寸重试

### 问题 3：小红书发布失败

**症状**：`Error: Publish failed`

**解决方案**：
1. 检查 Chrome 远程调试端口
2. 检查小红书登录状态
3. 检查发布内容是否违规
4. 检查发布频率是否过高

### 问题 4：数据收集失败

**症状**：`Error: Data collection failed`

**解决方案**：
1. 检查笔记 URL 是否正确
2. 检查网络连接
3. 检查小红书 API 权限
4. 手动验证数据

---

## ✅ 配置检查清单

### 火山引擎 API
- [ ] 已获取 API Key
- [ ] 已配置环境变量
- [ ] 已测试 API 连接
- [ ] 已测试图像生成

### Chrome 远程调试
- [ ] 已停止现有 Chrome 进程
- [ ] 已启动新的 Chrome（带远程调试）
- [ ] 已验证 9222 端口监听
- [ ] 已验证小红书登录状态

### 小红书自动化
- [ ] 已测试内容生成
- [ ] 已测试配图生成
- [ ] 已测试自动发布
- [ ] 已验证发布成功
- [ ] 已收集反馈数据

### 数据反馈
- [ ] 已实现数据收集
- [ ] 已实现数据分析
- [ ] 已生成数据报告
- [ ] 已实现优化建议

---

## 🎯 下一步行动

### 立即可做

1. **获取火山引擎 API Key** ⭐⭐⭐⭐⭐
   - 访问火山引擎控制台
   - 创建 API 密钥
   - 复制 API Key

2. **配置环境变量** ⭐⭐⭐⭐
   ```bash
   export VOLCENGINE_API_KEY="your_api_key"
   ```

3. **启动 Chrome 远程调试** ⭐⭐⭐⭐⭐
   ```bash
   google-chrome --remote-debugging-port=9222 --user-data-dir=/tmp/chrome-debug &
   ```

4. **测试图像生成** ⭐⭐⭐⭐
   ```bash
   python3 xhs-auto-pipeline.py test
   ```

5. **测试真实发布** ⭐⭐⭐⭐
   ```bash
   python3 xhs-auto-pipeline.py run --topic "AI工具"
   ```

---

**配置完成后告诉我，我帮你继续测试和优化！** 🐺
